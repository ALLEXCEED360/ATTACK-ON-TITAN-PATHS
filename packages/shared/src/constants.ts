// Dependency-free constants, importable by the web app as `@paths/shared/constants` without
// bringing Zod into the browser bundle.

/** Canon is manga chapters 1–139 (docs/canon-and-sources.md §1). */
export const FIRST_CHAPTER = 1;
export const LAST_CHAPTER = 139;
