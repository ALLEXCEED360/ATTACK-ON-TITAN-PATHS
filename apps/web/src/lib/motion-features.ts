// Motion's animation features, loaded after first paint (see main.tsx). `domAnimation` covers
// everything PATHS uses: animate, exit and variants — no drag or shared layout.
import { domAnimation } from "motion/react";

export default domAnimation;
